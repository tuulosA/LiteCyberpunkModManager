<UserControl x:Class="CyberpunkModManager.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Height="Auto" Width="Auto">

    <StackPanel Margin="20">
        <!-- Output Directory -->
        <TextBlock Text="Output Directory:" Margin="0,10,0,0"/>
        <TextBox Text="{Binding OutputDir, Mode=TwoWay}" Width="400" />

        <!-- Game Installation Directory -->
        <TextBlock Text="Game Installation Directory:" Margin="0,10,0,0"/>
        <TextBox Text="{Binding GameInstallationDir, Mode=TwoWay}" Width="400" />

        <!-- Nexus API Key -->
        <TextBlock Text="Nexus Mods API Key:" Margin="0,10,0,0"/>
        <PasswordBox x:Name="ApiKeyBox" Width="400" PasswordChanged="ApiKeyBox_PasswordChanged"/>

        <Button Content="Save Settings" Width="120" Margin="0,20,0,0" Click="SaveSettings_Click" />
    </StackPanel>
</UserControl>
